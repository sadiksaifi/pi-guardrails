# pi-guardrails

Permission guardrails for pi.

- Choose between `Default` and `Full Access`.
- Toggle quickly with `Alt+P` or use `/permissions`.
- Approve blocked actions once or grant a narrow session-scoped allowance when available.
- Sensitive actions always use a stricter prompt.
